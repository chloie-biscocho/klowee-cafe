{
  "//": "Copy the shape of this file. Do NOT put real secrets here. The real connection string belongs in `dotnet user-secrets` (see README). This example only documents the expected configuration keys.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "Default": "Host=db.<project-ref>.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=<your-password>;SSL Mode=Require;Trust Server Certificate=true"
  }
}
