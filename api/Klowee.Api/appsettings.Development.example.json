{
  "//": "Copy the shape of this file. Do NOT put real secrets here. Real values belong in `dotnet user-secrets` (see README). This example only documents the expected configuration keys.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "Default": "Host=db.<project-ref>.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=<your-password>;SSL Mode=Require;Trust Server Certificate=true"
  },
  "Jwt": {
    "//": "Key must be at least 32 characters. Generate one with: openssl rand -base64 48",
    "Key": "<at-least-32-characters-of-random-secret>",
    "Issuer": "klowee-cafe",
    "Audience": "klowee-cafe",
    "AccessTokenHours": 8
  },
  "Seed": {
    "//": "The two owner accounts, created by the seeder on Development startup. Passwords are hashed before storing; if any field is missing that owner is skipped with a warning.",
    "Owners": [
      {
        "Email": "<owner-one@example.com>",
        "Password": "<owner-one-password>",
        "DisplayName": "<Owner One>"
      },
      {
        "Email": "<owner-two@example.com>",
        "Password": "<owner-two-password>",
        "DisplayName": "<Owner Two>"
      }
    ]
  }
}
